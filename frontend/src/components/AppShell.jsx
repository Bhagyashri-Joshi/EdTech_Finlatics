import { NavLink, Outlet, useNavigate } from 'react-router-dom';
import { useState } from 'react';
import { useAuth } from '../lib/auth';
import { ArrowLeft, BarChart3, House, LogOut, Menu, Settings, Sparkles, Users, X } from 'lucide-react';
const nav=[['/dashboard',House,'Dashboard'],['/students',Users,'Students'],['/analytics',BarChart3,'Analytics'],['/ai-insights',Sparkles,'AI Insights'],['/settings',Settings,'Settings']];
export default function AppShell(){const {user,logout}=useAuth();const navigate=useNavigate();const [open,setOpen]=useState(false);const signOut=()=>{logout();navigate('/login')};return <div className="app-shell"><aside className={open?'sidebar open':'sidebar'}><div className="brand"><img className="brand-mark" src="/assets/logo.png" alt="EngageAI logo"/><div><b>EngageAI</b><small>Teacher Console</small></div><button className="close-menu" onClick={()=>setOpen(false)} aria-label="Close navigation"><X size={20}/></button></div><nav>{nav.map(([to,Icon,label])=><NavLink key={to} to={to} onClick={()=>setOpen(false)} className={({isActive})=>isActive?'active':''}><span><Icon size={18} strokeWidth={2} aria-hidden="true"/></span>{label}</NavLink>)}</nav><div className="sidebar-bottom"><div className="user-mini"><span>{user?.name?.[0]||'T'}</span><div><b>{user?.name}</b><small>{user?.role}</small></div></div><button onClick={signOut} className="logout"><LogOut size={16} aria-hidden="true"/> Logout</button></div></aside>{open&&<div className="sidebar-backdrop" onClick={()=>setOpen(false)}/>}<section className="app-content"><header className="topbar"><button className="menu-button" onClick={()=>setOpen(true)} aria-label="Open navigation"><Menu size={21}/></button><div><b>Student Engagement Platform</b><small>Live learner monitoring</small></div><div className="topbar-user"><span>{user?.name?.[0]||'T'}</span></div></header><main><Outlet/></main></section></div>}
